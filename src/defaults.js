export const defaultScene = {
  fov: 100, // vertical field of view
  near: 1, // near plane
  far: 5000, // far plane
  cameraY: 0, // initial camera start
  aspectX: window.innerWidth, // x aspect
  aspectY: window.innerHeight, // y aspect
}
